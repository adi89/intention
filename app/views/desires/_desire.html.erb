<%= turbo_frame_tag dom_id(desire) do %>
  <div class="card card-body">
    <div><%= desire.body %></div>

    <div class="mt-2">
      <%= link_to "Edit", edit_desire_path(desire) %>
      <%= button_to "+Prioritze", desire_prioritize_path(desire) %>
    </div>
  </div>
<% end %>
